/** TRACCC library, part of the ACTS project (R&D line)
 *
 * (c) 2026 CERN for the benefit of the ACTS project
 *
 * Mozilla Public License Version 2.0
 */

// Local include(s).
#include "src/finding/kernels/specializations/build_surface_flags_src.cuh"

// Project include(s).
#include "traccc/geometry/detector.hpp"

namespace traccc::cuda {
template void build_surface_flags<traccc::@DETECTOR_NAME@ ::device>(
    const dim3& grid_size, const dim3& block_size, const cudaStream_t& stream,
    traccc::@DETECTOR_NAME@ ::device::const_view_type det,
    const device::build_surface_flags_payload& payload);
}  // namespace traccc::cuda
